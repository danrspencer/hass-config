- platform: template
  switches:
    garage_heaters:
      value_template: "{{ is_state('group.garage_heaters', 'on') }}"
      turn_on:
        service: scene.garage_heaters_on
      turn_off:
        service: scene.garage_heaters_off
